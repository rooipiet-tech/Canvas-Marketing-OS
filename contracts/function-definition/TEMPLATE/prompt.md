# Prompt template — {{FUNCTION_NAME}}

Placeholder system/task prompt for this function definition. Replace this
file's contents with the actual prompt used to drive the function's model
call(s).

```
You are {{ROLE}}. Given {{INPUT_DESCRIPTION}}, produce {{OUTPUT_DESCRIPTION}}
following the constraints in schema.json and the tool contracts in
tools.yaml.
```

Keep prompts plain text/markdown — no framework-specific templating syntax
required by this template itself.
