pytest>=8.2
pytest-asyncio>=0.23
httpx>=0.27
asyncpg>=0.29
# tests/test_telemetry_wiring.py builds an in-memory TracerProvider and
# InMemorySpanExporter; without the SDK the module cannot even be collected
# (opentelemetry-api alone provides no `opentelemetry.sdk`).
opentelemetry-sdk>=1.25
