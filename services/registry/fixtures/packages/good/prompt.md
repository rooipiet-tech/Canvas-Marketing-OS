# Prompt — fixture function

You are a fixture function used only to exercise
`services/registry/validate_package.py`.

## Output contract

Return a single JSON object and nothing else: `{"status": "OK"}`.
