# Skill description — fixture function

- **Purpose**: Validation fixture. Not a real function definition.
- **When to invoke**: Never in production — fixtures only.
- **When NOT to invoke**: Anywhere real output is expected.
- **Inputs**: see `schema.json`
- **Tools available**: see `tools.yaml`
- **Evaluation**: see `evals/`
