function_id: 129
slug: web-reach-governor
# Appendix D PR 5c: the only card kind this function ever emits
# (source.allowlist) is security-tier -- non_negotiable, realtime, no
# default, no standing permission, ever (policies/autonomy-matrix.yaml).
# Level 0 ("Suggest": no external action, emits option cards only) is the
# closest fit; a rule PASS never reaches a card at all (auto-widened
# directly, citing SP-006). Was 2 (this package's own pre-wiring draft
# guess); corrected to match what actually ships.
autonomy_level: 0
reads:
- discovery_api_result
- crawler_snapshot
- allowlist_rule_config
- allowlist_deny_list
- docs/permission-register.yaml
- discovery_budget_config
writes:
- vault_asset
- option_card
never_writes:
- docs/permission-register.yaml
- docs/positioning.md
model_route: extract
max_tokens: 6144
drift_threshold: 0.15
