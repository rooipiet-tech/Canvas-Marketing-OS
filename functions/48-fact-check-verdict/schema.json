{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cmos.dev/schemas/function-definition/v1/48-fact-check-verdict/schema.json",
  "title": "Fact-Check Verdict I/O",
  "description": "Input/output contract for function 48, a FIRST DRAFT (6 Aug 2026, unreviewed by Pieter). Output is a blocking verdict: pass is true only when violations is empty. See prompt.md's Known limitation section before treating this as settled QA policy.",
  "type": "object",
  "properties": {
    "input": {
      "type": "object",
      "required": ["draft_text"],
      "additionalProperties": false,
      "properties": {
        "draft_text": {"type": "string", "minLength": 1},
        "client_references": {
          "type": "array",
          "items": {"type": "string", "minLength": 1},
          "description": "Not used by this function's own checks (client-naming clearance is function 02's job). Carried in the input shape only for symmetry with function 02's contract, since both are invoked by the same _single_draft_qa_review shared body in dispatch.py (round 34's per-draft replacement for the old _aggregate_qa_review)."
        },
        "channel": {
          "enum": ["linkedin", "web", "email", "deck", "internal-brief"],
          "default": "linkedin",
          "description": "Not currently used to change fact-check behaviour -- every channel is held to the same traceability bar. Present for symmetry with function 02's input contract."
        }
      }
    },
    "output": {
      "type": "object",
      "required": ["pass", "violations", "notes"],
      "additionalProperties": false,
      "properties": {
        "pass": {
          "type": "boolean",
          "description": "True only when violations is empty. No partial pass."
        },
        "violations": {
          "type": "array",
          "items": {
            "enum": [
              "fabricated-proof-point",
              "misstated-approved-fact",
              "revenue-model-misstatement"
            ]
          }
        },
        "notes": {"type": "string"}
      }
    }
  }
}
