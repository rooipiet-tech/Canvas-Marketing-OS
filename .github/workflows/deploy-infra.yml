name: deploy-infra

# Registration of Azure resource providers/features lives entirely in
# azure-provider-registration.yml (free, idempotent, never gated). This
# workflow's `what-if` job only starts once that workflow succeeds, and only
# the real deploy sits behind the cmos-dev approval gate — per the design
# rule: free idempotent preflight steps live outside approval gates so
# retries are unattended; only spend-triggering steps sit behind the gate.
#
# That real deploy is one of two mutually exclusive jobs, deploy-gated
# (environment: cmos-dev, requires approval) or deploy-autopass (no
# environment, runs unattended) — `what-if`'s "Determine autopass
# eligibility" step decides which, based on two signals: did this push
# touch any infra-defining file, and does what-if report any
# Create/Delete/Deploy/Unsupported change. See that step's own comment for
# why a naive "what-if reports zero changes" check doesn't work on this
# template (L-0034) and why both signals are required.

on:
  # Callable by deploy-pipeline.yml, which runs the whole cmos-dev deploy
  # in one fixed order instead of eight workflows racing in a shared
  # concurrency group. Inputs are read through the `inputs` context, which
  # is populated for workflow_dispatch AND workflow_call alike.
  workflow_call:
    inputs:
      environment_name:
        required: false
        type: string
        default: cmos-dev
  workflow_dispatch:
    inputs:
      environment_name:
        description: >
          Target environment. cmos-dev is the unchanged default path this
          session actually deploys to; cmos-prod is the additive
          promotion mechanism DE-1 authorizes as "prove the mechanism
          exists", NOT a live prod deploy -- selecting it here still only
          builds/validates against cmos-prod's own resource group/params,
          no cmos-prod deploy is executed by this session's own commits.
        required: false
        default: cmos-dev
        type: choice
        options:
          - cmos-dev
          - cmos-prod
# Concurrency is owned by deploy-pipeline.yml, the single caller that
# runs every deploy stage in one fixed order. A called workflow must
# NOT take the `cmos-dev-deploy` group its own caller already holds --
# it would wait forever on a group the parent cannot release until the
# child finishes. Pinned by test_deploy_pipeline_shape.py.

permissions:
  id-token: write
  contents: read

env:
  # DE-1/AC-13: same additive-parameterization shape as the concurrency
  # group above -- 'cmos-dev' for every existing trigger path, unchanged.
  RESOURCE_GROUP: ${{ (inputs.environment_name == 'cmos-prod' && 'cmos-prod') || 'cmos-dev' }}
  LOCATION: southafricanorth
  # DE-1/AC-13: infra/prod.parameters.json is a new sibling file to
  # dev.parameters.json (step 22) -- resolves to the unchanged
  # dev.parameters.json path for every existing trigger path.
  PARAMETERS_FILE: ${{ (inputs.environment_name == 'cmos-prod' && 'infra/prod.parameters.json') || 'infra/dev.parameters.json' }}

jobs:
  what-if:
    name: what-if
    # Inverted deliberately. Under `workflow_call` github.event_name is the
    # CALLER's event (push), never 'workflow_call' -- so the old allow-list
    # of event names evaluated false and would have skipped this job
    # silently, disabling the deploy while reporting green. Stated as "run
    # unless I was chained off a workflow_run that did not succeed", which
    # is identical on the workflow_run path and correct on the other three.
    if: >
      github.event_name != 'workflow_run' ||
      github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    # can_autopass feeds deploy-autopass/deploy-gated's mutually exclusive
    # `if:` conditions below — see the "Determine autopass eligibility" step
    # for what it actually checks and why (a naive "what-if reports zero
    # changes" gate never fires on this template at all; see L-0034).
    outputs:
      can_autopass: ${{ steps.autopass.outputs.can_autopass }}
    steps:
      # fetch-depth: 2 (not the default shallow depth-1 clone) so the
      # "did this push touch infra-defining files" check below can diff
      # against HEAD~1. `ref` pins to the EXACT commit that triggered the
      # upstream azure-provider-registration run (its head_sha) rather than
      # whatever main happens to be by the time this workflow starts —
      # falls back to github.sha for a manual workflow_dispatch, which has
      # no upstream run to inherit a ref from.
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
          fetch-depth: 2

      - uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Bootstrap az CLI extensions (non-interactive)
        run: |
          az config set extension.use_dynamic_install=yes_without_prompt
          az config set extension.dynamic_install_allow_preview=true
          az extension add --name containerapp --yes

      - name: Verify prerequisite registration (fast check, not a poll)
        run: |
          # Safety net for a manual workflow_dispatch that bypasses
          # azure-provider-registration.yml entirely: fail fast with a clear
          # message rather than letting `what-if`/`deploy` hit a confusing
          # MissingSubscriptionRegistration error deep into the run.
          MISSING=0
          for ns in Microsoft.App Microsoft.DBforPostgreSQL Microsoft.ServiceBus; do
            STATE=$(az provider show --namespace "$ns" --query registrationState -o tsv 2>/dev/null || echo "Unknown")
            echo "$ns registrationState = $STATE"
            if [ "$STATE" != "Registered" ]; then
              MISSING=1
            fi
          done
          # WorkloadProfiles feature-flag state is advisory only (see
          # .compound learning: AFEC flags for long-GA capabilities can wedge
          # in Pending indefinitely and may be vestigial) — checked and
          # logged, but does not gate this workflow. The real test is the
          # what-if/deploy outcome below: if it genuinely fails because of
          # this flag, revert this check to blocking.
          FEATURE_STATE=$(az feature show --namespace Microsoft.App --name WorkloadProfiles --query properties.state -o tsv 2>/dev/null || echo "Unknown")
          echo "Microsoft.App/WorkloadProfiles feature state = $FEATURE_STATE (advisory only)"
          if [ "$FEATURE_STATE" != "Registered" ]; then
            echo "::warning::WorkloadProfiles feature state is '$FEATURE_STATE', not 'Registered' — proceeding anyway (advisory only)."
          fi
          if [ "$MISSING" -ne 0 ]; then
            echo ""
            echo "Prerequisite provider/feature registration is not complete."
            echo "Run (or wait for) the 'azure-provider-registration' workflow first:"
            echo "  gh workflow run azure-provider-registration.yml"
            echo "Then re-run this workflow (or let its workflow_run trigger start it automatically on success)."
            exit 1
          fi

      - name: Create resource group (idempotent)
        run: az group create -n "$RESOURCE_GROUP" -l "$LOCATION"

      - name: Generate throwaway password for what-if
        id: pw
        run: |
          PG_PW=$(openssl rand -hex 32)
          echo "::add-mask::$PG_PW"
          echo "value=$PG_PW" >> "$GITHUB_OUTPUT"

      # ---------------------------------------------------------------
      # Resolve the model-gateway image for this deployment.
      #
      # gateway.bicep's containerImage parameter must never regress a real,
      # already-running gateway image back to the bootstrap placeholder on a
      # routine infra-only redeploy. If ca-model-gateway already exists AND
      # has ever produced a ready revision (latestReadyRevisionName is
      # non-empty), use its CURRENT live image (a no-op for the image
      # field); otherwise — first-ever provision, OR a prior deploy created
      # the app but its first revision never went healthy (e.g. it's still
      # stuck on a private-ACR image reference from before the identity had
      # pull rights) — bootstrap with a public, unauthenticated MCR
      # quickstart image that needs no dependency on our own registry at
      # all. Trusting the configured image string alone (without the
      # ready-revision check) reproduced deploy-infra's round-2 failure: a
      # partially-failed first deploy left the app pointing at an image that
      # had never actually pulled, and every subsequent run kept "preserving"
      # that same broken reference forever. deploy-gateway.yml remains the
      # only thing that ever sets a real gateway image, via
      # `az containerapp update --image` — this step never does that.
      # ---------------------------------------------------------------
      - name: Resolve gateway container image (preserve live image, bootstrap with placeholder)
        id: gateway_image
        run: |
          PLACEHOLDER="mcr.microsoft.com/azuredocs/containerapps-helloworld:latest"
          READY=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-model-gateway \
            --query "properties.latestReadyRevisionName" -o tsv 2>/dev/null || true)
          CURRENT=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-model-gateway \
            --query "properties.template.containers[0].image" -o tsv 2>/dev/null || true)
          if [ -n "$READY" ] && [ -n "$CURRENT" ]; then
            echo "ca-model-gateway has a ready revision ($READY) — preserving its current image: $CURRENT"
            echo "value=$CURRENT" >> "$GITHUB_OUTPUT"
          else
            echo "ca-model-gateway has no ready revision yet (latestReadyRevisionName='$READY') — bootstrapping with placeholder: $PLACEHOLDER"
            echo "value=$PLACEHOLDER" >> "$GITHUB_OUTPUT"
          fi

      # Same fix, same reasoning, for ca-orchestrator — see
      # infra/main.bicep's orchestratorContainerImage param header for the
      # incident this closes (run 30624109154: ca-orchestrator failed to
      # provision with MANIFEST_UNKNOWN because nothing before this step
      # ever verified an image tag actually existed at the registry this
      # template points at).
      - name: Resolve orchestrator container image (preserve live image, bootstrap with placeholder)
        id: orchestrator_image
        run: |
          PLACEHOLDER="mcr.microsoft.com/azuredocs/containerapps-helloworld:latest"
          READY=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-orchestrator \
            --query "properties.latestReadyRevisionName" -o tsv 2>/dev/null || true)
          CURRENT=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-orchestrator \
            --query "properties.template.containers[0].image" -o tsv 2>/dev/null || true)
          if [ -n "$READY" ] && [ -n "$CURRENT" ]; then
            echo "ca-orchestrator has a ready revision ($READY) — preserving its current image: $CURRENT"
            echo "value=$CURRENT" >> "$GITHUB_OUTPUT"
          else
            echo "ca-orchestrator has no ready revision yet (latestReadyRevisionName='$READY') — bootstrapping with placeholder: $PLACEHOLDER"
            echo "value=$PLACEHOLDER" >> "$GITHUB_OUTPUT"
          fi

      # Same fix, same reasoning, for ca-console — see console-app.bicep's
      # MAIDEN-DEPLOY INCIDENT header comment: ca-console's registries[]
      # (ACR pull identity) is set exclusively by deploy-console.yml's
      # `az containerapp registry set`, never by this template, so
      # bootstrapping with a public placeholder here needs no registry auth
      # at all on first create.
      - name: Resolve console container image (preserve live image, bootstrap with placeholder)
        id: console_image
        run: |
          PLACEHOLDER="mcr.microsoft.com/azuredocs/containerapps-helloworld:latest"
          READY=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-console \
            --query "properties.latestReadyRevisionName" -o tsv 2>/dev/null || true)
          CURRENT=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-console \
            --query "properties.template.containers[0].image" -o tsv 2>/dev/null || true)
          if [ -n "$READY" ] && [ -n "$CURRENT" ]; then
            echo "ca-console has a ready revision ($READY) — preserving its current image: $CURRENT"
            echo "value=$CURRENT" >> "$GITHUB_OUTPUT"
          else
            echo "ca-console has no ready revision yet (latestReadyRevisionName='$READY') — bootstrapping with placeholder: $PLACEHOLDER"
            echo "value=$PLACEHOLDER" >> "$GITHUB_OUTPUT"
          fi

      # Same fix, same reasoning, for the 3 mcp-* Container Apps — see
      # infra/main.bicep's mcpWebContainerImage param header for the
      # incident this closes (mcp-web/mcp-buffer/mcp-canva all failed to
      # provision with MANIFEST_UNKNOWN: none of their images had ever
      # been built).
      - name: Resolve mcp-web container image (preserve live image, bootstrap with placeholder)
        id: mcp_web_image
        run: |
          PLACEHOLDER="mcr.microsoft.com/azuredocs/containerapps-helloworld:latest"
          READY=$(az containerapp show -g "$RESOURCE_GROUP" -n mcp-web \
            --query "properties.latestReadyRevisionName" -o tsv 2>/dev/null || true)
          CURRENT=$(az containerapp show -g "$RESOURCE_GROUP" -n mcp-web \
            --query "properties.template.containers[0].image" -o tsv 2>/dev/null || true)
          if [ -n "$READY" ] && [ -n "$CURRENT" ]; then
            echo "mcp-web has a ready revision ($READY) — preserving its current image: $CURRENT"
            echo "value=$CURRENT" >> "$GITHUB_OUTPUT"
          else
            echo "mcp-web has no ready revision yet (latestReadyRevisionName='$READY') — bootstrapping with placeholder: $PLACEHOLDER"
            echo "value=$PLACEHOLDER" >> "$GITHUB_OUTPUT"
          fi

      - name: Resolve mcp-buffer container image (preserve live image, bootstrap with placeholder)
        id: mcp_buffer_image
        run: |
          PLACEHOLDER="mcr.microsoft.com/azuredocs/containerapps-helloworld:latest"
          READY=$(az containerapp show -g "$RESOURCE_GROUP" -n mcp-buffer \
            --query "properties.latestReadyRevisionName" -o tsv 2>/dev/null || true)
          CURRENT=$(az containerapp show -g "$RESOURCE_GROUP" -n mcp-buffer \
            --query "properties.template.containers[0].image" -o tsv 2>/dev/null || true)
          if [ -n "$READY" ] && [ -n "$CURRENT" ]; then
            echo "mcp-buffer has a ready revision ($READY) — preserving its current image: $CURRENT"
            echo "value=$CURRENT" >> "$GITHUB_OUTPUT"
          else
            echo "mcp-buffer has no ready revision yet (latestReadyRevisionName='$READY') — bootstrapping with placeholder: $PLACEHOLDER"
            echo "value=$PLACEHOLDER" >> "$GITHUB_OUTPUT"
          fi

      - name: Resolve mcp-canva container image (preserve live image, bootstrap with placeholder)
        id: mcp_canva_image
        run: |
          PLACEHOLDER="mcr.microsoft.com/azuredocs/containerapps-helloworld:latest"
          READY=$(az containerapp show -g "$RESOURCE_GROUP" -n mcp-canva \
            --query "properties.latestReadyRevisionName" -o tsv 2>/dev/null || true)
          CURRENT=$(az containerapp show -g "$RESOURCE_GROUP" -n mcp-canva \
            --query "properties.template.containers[0].image" -o tsv 2>/dev/null || true)
          if [ -n "$READY" ] && [ -n "$CURRENT" ]; then
            echo "mcp-canva has a ready revision ($READY) — preserving its current image: $CURRENT"
            echo "value=$CURRENT" >> "$GITHUB_OUTPUT"
          else
            echo "mcp-canva has no ready revision yet (latestReadyRevisionName='$READY') — bootstrapping with placeholder: $PLACEHOLDER"
            echo "value=$PLACEHOLDER" >> "$GITHUB_OUTPUT"
          fi

      # caj-mcp-smoke is a one-shot Manual-trigger JOB, not a long-running
      # app — it has no latestReadyRevisionName concept. "preserve if it
      # already exists with any image" is the job-shaped equivalent:
      # deploy-mcp.yml's gated deploy job is the only thing that ever sets
      # a real image here, and it always runs after this template is
      # applied (chained via workflow_run), so by the time this template
      # is re-applied for any later commit, caj-mcp-smoke's persisted image
      # is either already real (preserve it) or still the placeholder from
      # a not-yet-completed first bootstrap (also preserve it — re-writing
      # it to the same placeholder is a no-op either way).
      - name: Resolve mcp-smoke container image (preserve current, bootstrap with placeholder)
        id: mcp_smoke_image
        run: |
          PLACEHOLDER="mcr.microsoft.com/azuredocs/containerapps-helloworld:latest"
          CURRENT=$(az containerapp job show -g "$RESOURCE_GROUP" -n caj-mcp-smoke \
            --query "properties.template.containers[0].image" -o tsv 2>/dev/null || true)
          if [ -n "$CURRENT" ]; then
            echo "caj-mcp-smoke already exists — preserving its current image: $CURRENT"
            echo "value=$CURRENT" >> "$GITHUB_OUTPUT"
          else
            echo "caj-mcp-smoke does not exist yet — bootstrapping with placeholder: $PLACEHOLDER"
            echo "value=$PLACEHOLDER" >> "$GITHUB_OUTPUT"
          fi

      - name: az deployment group what-if
        run: |
          az deployment group what-if \
            -g "$RESOURCE_GROUP" \
            --template-file infra/main.bicep \
            --parameters @$PARAMETERS_FILE \
            --parameters administratorLoginPassword='${{ steps.pw.outputs.value }}' \
            --parameters gatewayContainerImage='${{ steps.gateway_image.outputs.value }}' \
            --parameters orchestratorContainerImage='${{ steps.orchestrator_image.outputs.value }}' \
            --parameters consoleContainerImage='${{ steps.console_image.outputs.value }}' \
            --parameters mcpWebContainerImage='${{ steps.mcp_web_image.outputs.value }}' \
            --parameters mcpBufferContainerImage='${{ steps.mcp_buffer_image.outputs.value }}' \
            --parameters mcpCanvaContainerImage='${{ steps.mcp_canva_image.outputs.value }}' \
            --parameters mcpSmokeContainerImage='${{ steps.mcp_smoke_image.outputs.value }}' \
            --parameters consoleClientId='00000000-0000-0000-0000-000000000000'

      # ---------------------------------------------------------------
      # AUTOPASS ELIGIBILITY — two independent signals, both required.
      #
      # A literal "what-if reports zero changes" gate never fires on this
      # template: live-verified (L-0034), what-if ALWAYS shows Modify on
      # the Postgres server (this workflow mints a fresh throwaway admin
      # password every run — see the "Generate throwaway password" step —
      # so what-if always sees a "changed" password), plus persistent
      # false-positive Modify noise on ServiceBus queues, private DNS zone
      # groups, and role assignments that Azure's own what-if engine cannot
      # diff cleanly. A gate keyed on "zero Modify entries" would simply
      # never trigger.
      #
      # Signal (a) — did this push touch infra-defining files at all
      # (`infra/**`, or the two deploy workflows themselves), compared to
      # the previous main commit. This is the precise "this merge didn't
      # change infrastructure" signal, and closes the gap signal (b) alone
      # would leave open: a genuine property-level edit (e.g. bumping a
      # container's CPU/memory in gateway.bicep) necessarily touches
      # infra/**, so it can never slip through as unreviewed "just Modify
      # noise" the way it could if autopass relied on what-if alone.
      #
      # Signal (b) — what-if reports no Create/Delete/Deploy/Unsupported
      # changes (the unambiguous "resource topology changed" signal,
      # distinct from the property-level Modify noise above). This is the
      # safety net: it catches drift that shows up at apply time even when
      # the DIFF looks clean (e.g. a resource that was deleted out-of-band
      # since the last deploy).
      #
      # Autopass requires BOTH — either signal alone gates. Every run logs
      # which signal(s) blocked or allowed autopass, so a gated run is
      # self-explanatory without re-deriving this reasoning from scratch.
      # ---------------------------------------------------------------
      - name: Check whether this push touched infra-defining files
        id: infra_diff
        run: |
          if git rev-parse HEAD~1 >/dev/null 2>&1; then
            CHANGED=$(git diff --name-only HEAD~1 HEAD -- infra/ .github/workflows/deploy-infra.yml .github/workflows/deploy-gateway.yml)
          else
            # No parent commit reachable (unexpected — fetch-depth: 2 above
            # should always provide one on a repo with any history). Fail
            # safe: treat as "changed" so the gate is never silently
            # skipped over a checkout anomaly rather than a real signal.
            CHANGED="(HEAD~1 unavailable — failing safe, treating as changed)"
          fi
          if [ -n "$CHANGED" ]; then
            echo "infra-defining files changed since the previous main commit:"
            echo "$CHANGED"
            echo "infra_changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "no infra-defining files changed since the previous main commit"
            echo "infra_changed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Check what-if for resource-topology changes (Create/Delete/Deploy)
        id: whatif_risk
        run: |
          RISKY=$(az deployment group what-if \
            -g "$RESOURCE_GROUP" \
            --template-file infra/main.bicep \
            --parameters @$PARAMETERS_FILE \
            --parameters administratorLoginPassword='${{ steps.pw.outputs.value }}' \
            --parameters gatewayContainerImage='${{ steps.gateway_image.outputs.value }}' \
            --parameters orchestratorContainerImage='${{ steps.orchestrator_image.outputs.value }}' \
            --parameters consoleContainerImage='${{ steps.console_image.outputs.value }}' \
            --parameters mcpWebContainerImage='${{ steps.mcp_web_image.outputs.value }}' \
            --parameters mcpBufferContainerImage='${{ steps.mcp_buffer_image.outputs.value }}' \
            --parameters mcpCanvaContainerImage='${{ steps.mcp_canva_image.outputs.value }}' \
            --parameters mcpSmokeContainerImage='${{ steps.mcp_smoke_image.outputs.value }}' \
            --parameters consoleClientId='00000000-0000-0000-0000-000000000000' \
            --no-pretty-print -o json --query "changes[?contains(['Create','Delete','Deploy','Unsupported'], changeType)]")
          if [ "$RISKY" != "[]" ] && [ -n "$RISKY" ]; then
            echo "what-if reports resource-topology changes (Create/Delete/Deploy/Unsupported):"
            echo "$RISKY"
            echo "risky_changes=true" >> "$GITHUB_OUTPUT"
          else
            echo "what-if reports no Create/Delete/Deploy/Unsupported changes (Modify-only noise, if any, does not block autopass)"
            echo "risky_changes=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Determine autopass eligibility
        id: autopass
        run: |
          INFRA_CHANGED="${{ steps.infra_diff.outputs.infra_changed }}"
          RISKY_CHANGES="${{ steps.whatif_risk.outputs.risky_changes }}"
          if [ "$INFRA_CHANGED" = "false" ] && [ "$RISKY_CHANGES" = "false" ]; then
            echo "AUTOPASS: no infra-defining files changed since the previous main commit, and what-if reports no resource-topology changes. Skipping the cmos-dev approval gate — see deploy-autopass below."
            echo "can_autopass=true" >> "$GITHUB_OUTPUT"
          else
            echo "GATED: infra_changed=$INFRA_CHANGED, risky_whatif_changes=$RISKY_CHANGES — at least one signal indicates this deploy needs human review. Routing to deploy-gated (requires cmos-dev environment approval)."
            echo "can_autopass=false" >> "$GITHUB_OUTPUT"
          fi

  # ---------------------------------------------------------------
  # deploy-gated and deploy-autopass run the IDENTICAL step sequence — a
  # YAML anchor (&deploy_steps / *deploy_steps) ties them to one literal
  # definition so the two can never drift out of sync with each other via a
  # copy-paste edit to just one of them. Verified this parses and expands
  # correctly via `python -c "import yaml; yaml.safe_load(...)"` before
  # relying on it (a real YAML 1.1 parser, the same class GitHub Actions'
  # own workflow processor uses — anchor/alias resolution happens at the
  # YAML-parsing stage, before any Actions-specific schema logic runs).
  #
  # Exactly one of the two runs per what-if execution — their `if:`
  # conditions are exact complements of `needs.what-if.outputs.can_autopass`
  # (see the "Determine autopass eligibility" step in what-if above for what
  # that actually checks and why a naive "what-if reports zero changes"
  # check doesn't work on this template).
  # ---------------------------------------------------------------
  deploy-gated:
    name: deploy (gated — requires cmos-dev approval)
    needs: what-if
    if: needs.what-if.outputs.can_autopass != 'true'
    runs-on: ubuntu-latest
    # DE-1/AC-13: same additive-parameterization shape as RESOURCE_GROUP/
    # concurrency-group above -- resolves to the unchanged 'cmos-dev' GH
    # Environment gate for every trigger this session exercises.
    environment: ${{ (inputs.environment_name == 'cmos-prod' && 'cmos-prod') || 'cmos-dev' }}
    steps: &deploy_steps
      - uses: actions/checkout@v4

      - uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Bootstrap az CLI extensions (non-interactive)
        run: |
          az config set extension.use_dynamic_install=yes_without_prompt
          az config set extension.dynamic_install_allow_preview=true
          az extension add --name containerapp --yes

      - name: Generate fresh Postgres admin password
        id: pw
        run: |
          PG_PW=$(openssl rand -hex 32)
          echo "::add-mask::$PG_PW"
          echo "value=$PG_PW" >> "$GITHUB_OUTPUT"

      # Same resolution as the what-if job above (see its comment) — this is
      # the copy that actually governs the real deploy.
      - name: Resolve gateway container image (preserve live image, bootstrap with placeholder)
        id: gateway_image
        run: |
          PLACEHOLDER="mcr.microsoft.com/azuredocs/containerapps-helloworld:latest"
          READY=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-model-gateway \
            --query "properties.latestReadyRevisionName" -o tsv 2>/dev/null || true)
          CURRENT=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-model-gateway \
            --query "properties.template.containers[0].image" -o tsv 2>/dev/null || true)
          if [ -n "$READY" ] && [ -n "$CURRENT" ]; then
            echo "ca-model-gateway has a ready revision ($READY) — preserving its current image: $CURRENT"
            echo "value=$CURRENT" >> "$GITHUB_OUTPUT"
          else
            echo "ca-model-gateway has no ready revision yet (latestReadyRevisionName='$READY') — bootstrapping with placeholder: $PLACEHOLDER"
            echo "value=$PLACEHOLDER" >> "$GITHUB_OUTPUT"
          fi

      # Same resolution as the what-if job above (see its comment) — this is
      # the copy that actually governs the real deploy.
      - name: Resolve orchestrator container image (preserve live image, bootstrap with placeholder)
        id: orchestrator_image
        run: |
          PLACEHOLDER="mcr.microsoft.com/azuredocs/containerapps-helloworld:latest"
          READY=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-orchestrator \
            --query "properties.latestReadyRevisionName" -o tsv 2>/dev/null || true)
          CURRENT=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-orchestrator \
            --query "properties.template.containers[0].image" -o tsv 2>/dev/null || true)
          if [ -n "$READY" ] && [ -n "$CURRENT" ]; then
            echo "ca-orchestrator has a ready revision ($READY) — preserving its current image: $CURRENT"
            echo "value=$CURRENT" >> "$GITHUB_OUTPUT"
          else
            echo "ca-orchestrator has no ready revision yet (latestReadyRevisionName='$READY') — bootstrapping with placeholder: $PLACEHOLDER"
            echo "value=$PLACEHOLDER" >> "$GITHUB_OUTPUT"
          fi

      # Same resolution as the what-if job's console_image step (see its
      # comment) — this is the copy that actually governs the real deploy.
      - name: Resolve console container image (preserve live image, bootstrap with placeholder)
        id: console_image
        run: |
          PLACEHOLDER="mcr.microsoft.com/azuredocs/containerapps-helloworld:latest"
          READY=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-console \
            --query "properties.latestReadyRevisionName" -o tsv 2>/dev/null || true)
          CURRENT=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-console \
            --query "properties.template.containers[0].image" -o tsv 2>/dev/null || true)
          if [ -n "$READY" ] && [ -n "$CURRENT" ]; then
            echo "ca-console has a ready revision ($READY) — preserving its current image: $CURRENT"
            echo "value=$CURRENT" >> "$GITHUB_OUTPUT"
          else
            echo "ca-console has no ready revision yet (latestReadyRevisionName='$READY') — bootstrapping with placeholder: $PLACEHOLDER"
            echo "value=$PLACEHOLDER" >> "$GITHUB_OUTPUT"
          fi

      # Same resolution as the what-if job's mcp_*_image steps (see their
      # comments) — these are the copies that actually govern the real
      # deploy.
      - name: Resolve mcp-web container image (preserve live image, bootstrap with placeholder)
        id: mcp_web_image
        run: |
          PLACEHOLDER="mcr.microsoft.com/azuredocs/containerapps-helloworld:latest"
          READY=$(az containerapp show -g "$RESOURCE_GROUP" -n mcp-web \
            --query "properties.latestReadyRevisionName" -o tsv 2>/dev/null || true)
          CURRENT=$(az containerapp show -g "$RESOURCE_GROUP" -n mcp-web \
            --query "properties.template.containers[0].image" -o tsv 2>/dev/null || true)
          if [ -n "$READY" ] && [ -n "$CURRENT" ]; then
            echo "mcp-web has a ready revision ($READY) — preserving its current image: $CURRENT"
            echo "value=$CURRENT" >> "$GITHUB_OUTPUT"
          else
            echo "mcp-web has no ready revision yet (latestReadyRevisionName='$READY') — bootstrapping with placeholder: $PLACEHOLDER"
            echo "value=$PLACEHOLDER" >> "$GITHUB_OUTPUT"
          fi

      - name: Resolve mcp-buffer container image (preserve live image, bootstrap with placeholder)
        id: mcp_buffer_image
        run: |
          PLACEHOLDER="mcr.microsoft.com/azuredocs/containerapps-helloworld:latest"
          READY=$(az containerapp show -g "$RESOURCE_GROUP" -n mcp-buffer \
            --query "properties.latestReadyRevisionName" -o tsv 2>/dev/null || true)
          CURRENT=$(az containerapp show -g "$RESOURCE_GROUP" -n mcp-buffer \
            --query "properties.template.containers[0].image" -o tsv 2>/dev/null || true)
          if [ -n "$READY" ] && [ -n "$CURRENT" ]; then
            echo "mcp-buffer has a ready revision ($READY) — preserving its current image: $CURRENT"
            echo "value=$CURRENT" >> "$GITHUB_OUTPUT"
          else
            echo "mcp-buffer has no ready revision yet (latestReadyRevisionName='$READY') — bootstrapping with placeholder: $PLACEHOLDER"
            echo "value=$PLACEHOLDER" >> "$GITHUB_OUTPUT"
          fi

      - name: Resolve mcp-canva container image (preserve live image, bootstrap with placeholder)
        id: mcp_canva_image
        run: |
          PLACEHOLDER="mcr.microsoft.com/azuredocs/containerapps-helloworld:latest"
          READY=$(az containerapp show -g "$RESOURCE_GROUP" -n mcp-canva \
            --query "properties.latestReadyRevisionName" -o tsv 2>/dev/null || true)
          CURRENT=$(az containerapp show -g "$RESOURCE_GROUP" -n mcp-canva \
            --query "properties.template.containers[0].image" -o tsv 2>/dev/null || true)
          if [ -n "$READY" ] && [ -n "$CURRENT" ]; then
            echo "mcp-canva has a ready revision ($READY) — preserving its current image: $CURRENT"
            echo "value=$CURRENT" >> "$GITHUB_OUTPUT"
          else
            echo "mcp-canva has no ready revision yet (latestReadyRevisionName='$READY') — bootstrapping with placeholder: $PLACEHOLDER"
            echo "value=$PLACEHOLDER" >> "$GITHUB_OUTPUT"
          fi

      # Same resolution as the what-if job's mcp_smoke_image step (see its
      # comment) — this is the copy that actually governs the real deploy.
      - name: Resolve mcp-smoke container image (preserve current, bootstrap with placeholder)
        id: mcp_smoke_image
        run: |
          PLACEHOLDER="mcr.microsoft.com/azuredocs/containerapps-helloworld:latest"
          CURRENT=$(az containerapp job show -g "$RESOURCE_GROUP" -n caj-mcp-smoke \
            --query "properties.template.containers[0].image" -o tsv 2>/dev/null || true)
          if [ -n "$CURRENT" ]; then
            echo "caj-mcp-smoke already exists — preserving its current image: $CURRENT"
            echo "value=$CURRENT" >> "$GITHUB_OUTPUT"
          else
            echo "caj-mcp-smoke does not exist yet — bootstrapping with placeholder: $PLACEHOLDER"
            echo "value=$PLACEHOLDER" >> "$GITHUB_OUTPUT"
          fi

      - name: az deployment group create (real deploy)
        run: |
          az deployment group create \
            -g "$RESOURCE_GROUP" \
            --template-file infra/main.bicep \
            --parameters @$PARAMETERS_FILE \
            --parameters administratorLoginPassword='${{ steps.pw.outputs.value }}' \
            --parameters gatewayContainerImage='${{ steps.gateway_image.outputs.value }}' \
            --parameters orchestratorContainerImage='${{ steps.orchestrator_image.outputs.value }}' \
            --parameters consoleContainerImage='${{ steps.console_image.outputs.value }}' \
            --parameters mcpWebContainerImage='${{ steps.mcp_web_image.outputs.value }}' \
            --parameters mcpBufferContainerImage='${{ steps.mcp_buffer_image.outputs.value }}' \
            --parameters mcpCanvaContainerImage='${{ steps.mcp_canva_image.outputs.value }}' \
            --parameters mcpSmokeContainerImage='${{ steps.mcp_smoke_image.outputs.value }}' \
            --parameters consoleClientId="${{ secrets.CONSOLE_ENTRA_CLIENT_ID || '00000000-0000-0000-0000-000000000000' }}"

      - name: Start Vault-schema migration job
        run: az containerapp job start -g "$RESOURCE_GROUP" -n caj-vault-migrate

      - name: Poll migration job execution to Succeeded
        run: |
          MAX_ATTEMPTS=20
          SLEEP_SECONDS=15
          ATTEMPT=1
          STATUS=""
          while [ "$ATTEMPT" -le "$MAX_ATTEMPTS" ]; do
            STATUS=$(az containerapp job execution list -g "$RESOURCE_GROUP" -n caj-vault-migrate --query "[0].properties.status" -o tsv 2>/dev/null || echo "Unknown")
            echo "Attempt $ATTEMPT/$MAX_ATTEMPTS: migration job status = $STATUS"
            if [ "$STATUS" = "Succeeded" ] || [ "$STATUS" = "Failed" ]; then
              break
            fi
            ATTEMPT=$((ATTEMPT + 1))
            if [ "$ATTEMPT" -le "$MAX_ATTEMPTS" ]; then
              sleep "$SLEEP_SECONDS"
            fi
          done
          if [ "$STATUS" != "Succeeded" ]; then
            echo "caj-vault-migrate did not reach Succeeded (last status: $STATUS)"
            # Show ground truth in the run that failed (L-0064) — deploy-mcp.yml and
            # deploy-governance.yml already do this. `|| true` keeps a log-fetch
            # failure from masking the exit 1. L-0024: --container is required.
            EXECUTION_NAME=$(az containerapp job execution list -g "$RESOURCE_GROUP" -n caj-vault-migrate --query "[0].name" -o tsv 2>/dev/null || echo "")
            echo "--- caj-vault-migrate log (execution: $EXECUTION_NAME) ---"
            az containerapp job logs show -g "$RESOURCE_GROUP" -n caj-vault-migrate --execution "$EXECUTION_NAME" --container vault-migrate || true
            echo "--- end of caj-vault-migrate log ---"
            exit 1
          fi

      - name: Re-check Postgres public network access remains Disabled
        run: |
          PG=$(az resource list -g "$RESOURCE_GROUP" --resource-type Microsoft.DBforPostgreSQL/flexibleServers --query '[0].name' -o tsv)
          ACCESS=$(az postgres flexible-server show -g "$RESOURCE_GROUP" -n "$PG" --query network.publicNetworkAccess -o tsv)
          echo "Postgres publicNetworkAccess = $ACCESS"
          test "$ACCESS" = "Disabled"

      # -- session/s2-vault: begin --
      # Vault deploy sequence: secret-writer (loads vault-db-connection-string
      # into Key Vault from inside the VNet — never opens publicNetworkAccess,
      # L-0012) -> sidecar-migration (applies vault_internal schema) ->
      # smoke-test (the full test_contract_smoke.py suite against ca-vault's
      # internal-ingress endpoint). Each job is started then polled to a
      # terminal state with a bounded attempt count (L-0006); the deploy job
      # fails if the smoke-test job does not reach Succeeded.
      - name: Re-check Key Vault public network access remains Disabled
        run: |
          KV=$(az resource list -g "$RESOURCE_GROUP" --resource-type Microsoft.KeyVault/vaults --query '[0].name' -o tsv)
          ACCESS=$(az keyvault show -g "$RESOURCE_GROUP" -n "$KV" --query properties.publicNetworkAccess -o tsv)
          echo "Key Vault publicNetworkAccess = $ACCESS"
          test "$ACCESS" = "Disabled"

      # ACR MIGRATION verification (L-0021 class): confirms ca-vault is
      # actually pulling its image from the canonical shared ACR
      # (container-registry.bicep's module, the same registry
      # ca-model-gateway uses) rather than the orphaned pre-convention
      # acrcmosdev registry vault used to be provisioned against. This is
      # the "verify the app runs from the new image" gate the acrcmosdev...
      # registry's eventual decommission depends on; this workflow does not
      # delete that registry itself (a live Azure resource deletion is a
      # human call, not automated here). Readiness itself (latestReady vs
      # latest) is checked separately below, as a bounded poll rather than
      # a one-shot check — see that step's comment for why.
      - name: Verify ca-vault is running from the canonical shared ACR
        run: |
          set -e
          CANONICAL=$(az deployment group show -g "$RESOURCE_GROUP" -n container-registry --query "properties.outputs.loginServer.value" -o tsv)
          test -n "$CANONICAL" || { echo "could not resolve the canonical registry from the container-registry deployment output"; exit 1; }
          LIVE_IMAGE=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-vault --query "properties.template.containers[0].image" -o tsv)
          echo "canonical registry:  $CANONICAL"
          echo "ca-vault live image: $LIVE_IMAGE"
          case "$LIVE_IMAGE" in
            "$CANONICAL"/*) echo "ca-vault is pulling from the canonical shared ACR." ;;
            *) echo "ca-vault's image ($LIVE_IMAGE) is not on the canonical registry ($CANONICAL) — ACR migration incomplete"; exit 1 ;;
          esac

      # ca-vault/container-app.bicep now carries a `deployToken`-driven
      # revisionSuffix (same governance-round-4 pattern as
      # gatekeeper-app.bicep/publisher-app.bicep — see its header comment):
      # every deploy genuinely creates a new revision now, not just when the
      # image tag changes, so a rotated Postgres-adjacent secret value can
      # never strand ca-vault on a stale revision. That in turn means a new
      # revision is not instantly Ready (image pull, health probe warm-up),
      # so wait for latestReadyRevisionName to catch up to latestRevisionName
      # BEFORE the secret-writer/sidecar-migration/smoke-test sequence below
      # — mirroring deploy-governance.yml's "Wait for governance app
      # revisions to be Ready" step for ca-gatekeeper/ca-publisher.
      - name: Wait for ca-vault revision to be Ready
        run: |
          LATEST=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-vault --query "properties.latestRevisionName" -o tsv)
          echo "ca-vault latestRevisionName=$LATEST"
          MAX_ATTEMPTS=20
          SLEEP_SECONDS=15
          ATTEMPT=1
          READY=""
          while [ "$ATTEMPT" -le "$MAX_ATTEMPTS" ]; do
            READY=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-vault --query "properties.latestReadyRevisionName" -o tsv 2>/dev/null || true)
            echo "Attempt $ATTEMPT/$MAX_ATTEMPTS: ca-vault latestReadyRevisionName=$READY (waiting for $LATEST)"
            if [ "$READY" = "$LATEST" ]; then
              break
            fi
            ATTEMPT=$((ATTEMPT + 1))
            if [ "$ATTEMPT" -le "$MAX_ATTEMPTS" ]; then
              sleep "$SLEEP_SECONDS"
            fi
          done
          if [ "$READY" != "$LATEST" ]; then
            echo "ca-vault never reported $LATEST as its latestReadyRevisionName (last seen: $READY)"
            exit 1
          fi

      - name: Start Vault secret-writer job (loads vault-db-connection-string in-VNet)
        run: az containerapp job start -g "$RESOURCE_GROUP" -n caj-vault-secret-writer

      - name: Poll secret-writer job execution to Succeeded
        run: |
          MAX_ATTEMPTS=20
          SLEEP_SECONDS=15
          ATTEMPT=1
          STATUS=""
          while [ "$ATTEMPT" -le "$MAX_ATTEMPTS" ]; do
            STATUS=$(az containerapp job execution list -g "$RESOURCE_GROUP" -n caj-vault-secret-writer --query "[0].properties.status" -o tsv 2>/dev/null || echo "Unknown")
            echo "Attempt $ATTEMPT/$MAX_ATTEMPTS: secret-writer job status = $STATUS"
            if [ "$STATUS" = "Succeeded" ] || [ "$STATUS" = "Failed" ]; then
              break
            fi
            ATTEMPT=$((ATTEMPT + 1))
            if [ "$ATTEMPT" -le "$MAX_ATTEMPTS" ]; then
              sleep "$SLEEP_SECONDS"
            fi
          done
          if [ "$STATUS" != "Succeeded" ]; then
            echo "caj-vault-secret-writer did not reach Succeeded (last status: $STATUS)"
            # Show ground truth in the run that failed (L-0064) — deploy-mcp.yml and
            # deploy-governance.yml already do this. `|| true` keeps a log-fetch
            # failure from masking the exit 1. L-0024: --container is required.
            EXECUTION_NAME=$(az containerapp job execution list -g "$RESOURCE_GROUP" -n caj-vault-secret-writer --query "[0].name" -o tsv 2>/dev/null || echo "")
            echo "--- caj-vault-secret-writer log (execution: $EXECUTION_NAME) ---"
            az containerapp job logs show -g "$RESOURCE_GROUP" -n caj-vault-secret-writer --execution "$EXECUTION_NAME" --container vault-secret-writer || true
            echo "--- end of caj-vault-secret-writer log ---"
            exit 1
          fi

      - name: Start Vault sidecar-migration job (vault_internal schema)
        run: az containerapp job start -g "$RESOURCE_GROUP" -n caj-vault-sidecar-migrate

      - name: Poll sidecar-migration job execution to Succeeded
        run: |
          MAX_ATTEMPTS=20
          SLEEP_SECONDS=15
          ATTEMPT=1
          STATUS=""
          while [ "$ATTEMPT" -le "$MAX_ATTEMPTS" ]; do
            STATUS=$(az containerapp job execution list -g "$RESOURCE_GROUP" -n caj-vault-sidecar-migrate --query "[0].properties.status" -o tsv 2>/dev/null || echo "Unknown")
            echo "Attempt $ATTEMPT/$MAX_ATTEMPTS: sidecar-migration job status = $STATUS"
            if [ "$STATUS" = "Succeeded" ] || [ "$STATUS" = "Failed" ]; then
              break
            fi
            ATTEMPT=$((ATTEMPT + 1))
            if [ "$ATTEMPT" -le "$MAX_ATTEMPTS" ]; then
              sleep "$SLEEP_SECONDS"
            fi
          done
          if [ "$STATUS" != "Succeeded" ]; then
            echo "caj-vault-sidecar-migrate did not reach Succeeded (last status: $STATUS)"
            # Show ground truth in the run that failed (L-0064) — deploy-mcp.yml and
            # deploy-governance.yml already do this. `|| true` keeps a log-fetch
            # failure from masking the exit 1. L-0024: --container is required.
            EXECUTION_NAME=$(az containerapp job execution list -g "$RESOURCE_GROUP" -n caj-vault-sidecar-migrate --query "[0].name" -o tsv 2>/dev/null || echo "")
            echo "--- caj-vault-sidecar-migrate log (execution: $EXECUTION_NAME) ---"
            az containerapp job logs show -g "$RESOURCE_GROUP" -n caj-vault-sidecar-migrate --execution "$EXECUTION_NAME" --container vault-sidecar-migrate || true
            echo "--- end of caj-vault-sidecar-migrate log ---"
            exit 1
          fi

      - name: Start Vault smoke-test job (full test_contract_smoke.py suite)
        run: az containerapp job start -g "$RESOURCE_GROUP" -n caj-vault-smoke-test

      - name: Poll smoke-test job execution to Succeeded (fails the deploy on smoke-test failure)
        run: |
          MAX_ATTEMPTS=40
          SLEEP_SECONDS=15
          ATTEMPT=1
          STATUS=""
          while [ "$ATTEMPT" -le "$MAX_ATTEMPTS" ]; do
            STATUS=$(az containerapp job execution list -g "$RESOURCE_GROUP" -n caj-vault-smoke-test --query "[0].properties.status" -o tsv 2>/dev/null || echo "Unknown")
            echo "Attempt $ATTEMPT/$MAX_ATTEMPTS: smoke-test job status = $STATUS"
            if [ "$STATUS" = "Succeeded" ] || [ "$STATUS" = "Failed" ]; then
              break
            fi
            ATTEMPT=$((ATTEMPT + 1))
            if [ "$ATTEMPT" -le "$MAX_ATTEMPTS" ]; then
              sleep "$SLEEP_SECONDS"
            fi
          done
          if [ "$STATUS" != "Succeeded" ]; then
            echo "caj-vault-smoke-test did not reach Succeeded (last status: $STATUS) — failing deploy"
            # Dump the suite output here rather than naming a command for someone
            # to run later — the pytest failure is the whole diagnosis and it has
            # to be in the run that failed (L-0064). L-0024: --container is
            # required even for a single-container job (smoke-test-job.bicep
            # declares it as 'vault-smoke-test').
            EXECUTION_NAME=$(az containerapp job execution list -g "$RESOURCE_GROUP" -n caj-vault-smoke-test --query "[0].name" -o tsv 2>/dev/null || echo "")
            echo "--- caj-vault-smoke-test log (execution: $EXECUTION_NAME) ---"
            az containerapp job logs show -g "$RESOURCE_GROUP" -n caj-vault-smoke-test --execution "$EXECUTION_NAME" --container vault-smoke-test || true
            echo "--- end of caj-vault-smoke-test log ---"
            exit 1
          fi
      # -- session/s2-vault: end --

      # -- session/s3-orchestrator: begin --
      # Append-only block (C10): starts the orchestrator schema migration
      # job, then the live heartbeat-injection-and-golden-compare smoke
      # test job (AC-028), each polled to a terminal state with a bounded
      # attempt count (mirrors caj-vault-migrate's own poll pattern above —
      # a bounded-attempt loop only, never an unconditional/unbounded
      # poll, per L-0006). This block runs inside the `deploy` job, which
      # already sits behind the
      # `environment: cmos-dev` approval gate (AC-035).
      #
      # ca-orchestrator/container-app.bicep now carries a `deployToken`-driven
      # revisionSuffix (same governance-round-4 pattern as ca-vault/
      # ca-gatekeeper/ca-publisher — see container-app.bicep's header
      # comment): every deploy genuinely creates a new revision, not just
      # when the image tag changes, so a rotated Postgres-adjacent secret
      # value can never strand ca-orchestrator on a stale revision. That in
      # turn means a new revision is not instantly Ready (image pull,
      # health probe warm-up), so wait for latestReadyRevisionName to catch
      # up to latestRevisionName BEFORE the migration/smoke-test sequence
      # below — mirroring the "Wait for ca-vault revision to be Ready" step
      # above.
      - name: Wait for ca-orchestrator revision to be Ready
        run: |
          LATEST=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-orchestrator --query "properties.latestRevisionName" -o tsv)
          echo "ca-orchestrator latestRevisionName=$LATEST"
          MAX_ATTEMPTS=20
          SLEEP_SECONDS=15
          ATTEMPT=1
          READY=""
          while [ "$ATTEMPT" -le "$MAX_ATTEMPTS" ]; do
            READY=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-orchestrator --query "properties.latestReadyRevisionName" -o tsv 2>/dev/null || true)
            echo "Attempt $ATTEMPT/$MAX_ATTEMPTS: ca-orchestrator latestReadyRevisionName=$READY (waiting for $LATEST)"
            if [ "$READY" = "$LATEST" ]; then
              break
            fi
            ATTEMPT=$((ATTEMPT + 1))
            if [ "$ATTEMPT" -le "$MAX_ATTEMPTS" ]; then
              sleep "$SLEEP_SECONDS"
            fi
          done
          if [ "$READY" != "$LATEST" ]; then
            echo "ca-orchestrator never reported $LATEST as its latestReadyRevisionName (last seen: $READY)"
            exit 1
          fi

      # INCIDENT (2026-08-03, live — escalation 9, deploy-loop-e2e-smoke
      # #14): infra/modules/orchestrator/container-app.bicep's `env` block
      # only ever declares VAULT_API_URL (default '') — it has no
      # CMOS_MCP_WEB_BASE_URL/CMOS_GATEWAY_BASE_URL/CMOS_GATEKEEPER_BASE_URL
      # params at all. The ONLY thing that has ever set the real values for
      # all 4 of these is orchestrator-image.yml's own "Wire live FQDNs..."
      # deploy step — which only runs on a push to services/orchestrator/**
      # or a manual dispatch of THAT workflow, never as part of this one.
      # Every deploy-infra run does a full `az deployment group create`
      # PUT of ca-orchestrator's container app — Container Apps revisions
      # are full template snapshots, so this silently wipes out whatever
      # orchestrator-image.yml had previously wired in, back down to
      # Bicep's bare 3-var template, the moment this workflow runs without
      # orchestrator-image.yml having *just* re-run right after it.
      # Confirmed live: every dispatched loop task failed with
      # `MCPClient requires a resolved base_url` / `VaultClientExt
      # requires a resolved base_url` (orchestrator/clients/*_client*.py's
      # own L-0025 guard, working exactly as designed against an empty
      # env var) after deploy-infra #64 ran with no following
      # orchestrator-image.yml run. Same root bug class as L-0065
      # (a workflow deploying a shared template silently resets state only
      # a DIFFERENT, sibling workflow's out-of-band step ever sets) — but
      # for env vars pointing at another app's live ingress FQDN, not a
      # container image tag. Unlike an image tag (which deploy-infra
      # cannot know ahead of a separate CI build), a dependent app's
      # ingress FQDN can be freshly queried right here, every time, with
      # no "preserve the prior value" semantics needed at all — copied
      # verbatim from orchestrator-image.yml's own step rather than
      # threading 4 new preserve-or-placeholder Bicep params through
      # main.bicep, since there is nothing here worth preserving over a
      # fresh query. Runs after ca-orchestrator's revision is confirmed
      # Ready (the wait step immediately above) so the FQDN update lands
      # on a serving revision, not a still-activating one.
      - name: Wire live FQDNs for orchestrator's 4 dependent services as env vars
        run: |
          set -e
          declare -A APP_ENV_MAP=(
            ["ca-vault"]="VAULT_API_URL"
            ["ca-model-gateway"]="CMOS_GATEWAY_BASE_URL"
            ["mcp-web"]="CMOS_MCP_WEB_BASE_URL"
            ["ca-gatekeeper"]="CMOS_GATEKEEPER_BASE_URL"
          )
          SET_ARGS=()
          for APP_NAME in "${!APP_ENV_MAP[@]}"; do
            ENV_VAR="${APP_ENV_MAP[$APP_NAME]}"
            FQDN=$(az containerapp show -g "$RESOURCE_GROUP" -n "$APP_NAME" --query "properties.configuration.ingress.fqdn" -o tsv 2>/dev/null || true)
            if [ -z "$FQDN" ]; then
              echo "::warning::$APP_NAME has no live ingress FQDN yet -- skipping $ENV_VAR (will be wired on a future run of this workflow once $APP_NAME exists)"
              continue
            fi
            echo "$APP_NAME -> $ENV_VAR=https://$FQDN"
            SET_ARGS+=("${ENV_VAR}=https://${FQDN}")
          done
          if [ "${#SET_ARGS[@]}" -eq 0 ]; then
            echo "no dependent-service FQDNs resolved -- nothing to wire, leaving ca-orchestrator's env vars unchanged"
            exit 0
          fi
          az containerapp update -n ca-orchestrator -g "$RESOURCE_GROUP" \
            --set-env-vars "${SET_ARGS[@]}"

      - name: Start orchestrator schema migration job
        run: az containerapp job start -g "$RESOURCE_GROUP" -n caj-orchestrator-migrate

      - name: Poll orchestrator migration job execution to Succeeded
        run: |
          MAX_ATTEMPTS=20
          SLEEP_SECONDS=15
          ATTEMPT=1
          STATUS=""
          while [ "$ATTEMPT" -le "$MAX_ATTEMPTS" ]; do
            STATUS=$(az containerapp job execution list -g "$RESOURCE_GROUP" -n caj-orchestrator-migrate --query "[0].properties.status" -o tsv 2>/dev/null || echo "Unknown")
            echo "Attempt $ATTEMPT/$MAX_ATTEMPTS: orchestrator migration job status = $STATUS"
            if [ "$STATUS" = "Succeeded" ] || [ "$STATUS" = "Failed" ]; then
              break
            fi
            ATTEMPT=$((ATTEMPT + 1))
            if [ "$ATTEMPT" -le "$MAX_ATTEMPTS" ]; then
              sleep "$SLEEP_SECONDS"
            fi
          done
          if [ "$STATUS" != "Succeeded" ]; then
            echo "caj-orchestrator-migrate did not reach Succeeded (last status: $STATUS)"
            # L-0024: job logs show requires an explicit --container even for
            # a single-container job (matches migration-job.bicep's declared
            # container name 'orchestrator-migrate').
            EXECUTION_NAME=$(az containerapp job execution list -g "$RESOURCE_GROUP" -n caj-orchestrator-migrate --query "[0].name" -o tsv 2>/dev/null || echo "")
            echo "--- caj-orchestrator-migrate log (execution: $EXECUTION_NAME) ---"
            az containerapp job logs show -g "$RESOURCE_GROUP" -n caj-orchestrator-migrate --execution "$EXECUTION_NAME" --container orchestrator-migrate || true
            echo "--- end of caj-orchestrator-migrate log ---"
            exit 1
          fi

      # INCIDENT (2026-07-31, execution caj-orchestrator-smoke-test-e7e7956):
      # this job ran to completion in ~13s — "Successfully pulled image
      # 'mcr.microsoft.com/azuredocs/containerapps-helloworld:latest'" then
      # "Container 'orchestrator-smoke-test' was terminated with exit code
      # '1' ... exec: \"python\": executable file not found in $PATH"
      # (confirmed live via ContainerAppSystemLogs_CL) — because
      # ca-orchestrator/caj-orchestrator-smoke-test were BOTH still on the
      # bootstrap placeholder image (steps.orchestrator_image resolved to it
      # since ca-orchestrator had no Ready revision on a real image yet;
      # orchestrator-image.yml's gated deploy job — the only thing that ever
      # sets a real image, see infra/main.bicep's orchestratorContainerImage
      # param header — hadn't run). The hello-world placeholder has no
      # Python interpreter, so `python -m orchestrator.smoke_test` can never
      # execute against it, ever. But Container Apps Jobs does not mark the
      # EXECUTION "Failed" until the full replicaTimeout (900s,
      # smoke-test-job.bicep) elapses via "DeadlineExceeded" — so the poll
      # below (a 600s budget) silently burned its whole budget reporting
      # "still Running" for a failure that actually happened in 13s.
      # ContainerAppConsoleLogs_CL/ServiceBus queue counts (0 active
      # messages on both `event`/`task`) confirmed the synthetic heartbeat
      # never even got published — the container crashed before any Python
      # ran.
      #
      # This precondition (real image or bust) is knowable BEFORE ever
      # starting the job — steps.orchestrator_image already resolved it
      # earlier in this same job — so check it here and fail in <1s with a
      # clear diagnostic instead of starting a job we already know cannot
      # succeed.
      - name: "Guard: refuse to run the live smoke test against the bootstrap placeholder image"
        run: |
          PLACEHOLDER="mcr.microsoft.com/azuredocs/containerapps-helloworld:latest"
          CURRENT='${{ steps.orchestrator_image.outputs.value }}'
          if [ "$CURRENT" = "$PLACEHOLDER" ]; then
            echo "::error::ca-orchestrator is still running the bootstrap placeholder image ($PLACEHOLDER) — it has never received a real orchestrator image."
            echo "caj-orchestrator-smoke-test runs 'python -m orchestrator.smoke_test' inside whatever image ca-orchestrator currently uses; against the placeholder this fails in ~13s with 'exec: \"python\": executable file not found in \$PATH', but the job EXECUTION doesn't report Failed until the full 900s replicaTimeout elapses — so running it here would just silently burn this poll's whole budget again."
            echo "A real image is only ever set by orchestrator-image.yml's gated 'deploy' job (az containerapp update --image ...) — see infra/main.bicep's orchestratorContainerImage param header."
            echo "Check whether that workflow has run and been approved:"
            echo "  gh run list --workflow=orchestrator-image.yml --limit 5"
            echo "If it hasn't, trigger it (workflow_dispatch is one of its triggers), approve the cmos-dev environment gate on its deploy job, then re-run this workflow."
            exit 1
          fi
          echo "ca-orchestrator's resolved image is not the placeholder ($CURRENT) — proceeding with the live smoke test."

      - name: Start orchestrator live smoke test job (AC-028)
        run: az containerapp job start -g "$RESOURCE_GROUP" -n caj-orchestrator-smoke-test

      - name: Poll orchestrator smoke test job execution to Succeeded
        run: |
          MAX_ATTEMPTS=40
          SLEEP_SECONDS=15
          ATTEMPT=1
          STATUS=""
          while [ "$ATTEMPT" -le "$MAX_ATTEMPTS" ]; do
            STATUS=$(az containerapp job execution list -g "$RESOURCE_GROUP" -n caj-orchestrator-smoke-test --query "[0].properties.status" -o tsv 2>/dev/null || echo "Unknown")
            echo "Attempt $ATTEMPT/$MAX_ATTEMPTS: orchestrator smoke test job status = $STATUS"
            # Diagnostic checkpoint every 10 attempts (~150s) — the guard
            # above already rules out the one KNOWN cause of a silent hang
            # (placeholder image), so a genuinely new hang past this point
            # is a different bug; print live signal on what's actually
            # happening instead of a bare, unexplained "still Running".
            if [ $((ATTEMPT % 10)) -eq 0 ] && [ "$STATUS" != "Succeeded" ] && [ "$STATUS" != "Failed" ]; then
              echo "--- diagnostic at attempt $ATTEMPT/$MAX_ATTEMPTS: what caj-orchestrator-smoke-test may still be waiting for ---"
              CURRENT_IMAGE=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-orchestrator --query "properties.template.containers[0].image" -o tsv 2>/dev/null || echo "unknown")
              CURRENT_READY=$(az containerapp show -g "$RESOURCE_GROUP" -n ca-orchestrator --query "properties.latestReadyRevisionName" -o tsv 2>/dev/null || echo "unknown")
              echo "ca-orchestrator current image: $CURRENT_IMAGE (latestReadyRevisionName: $CURRENT_READY)"
              echo "If this still says the placeholder image, the guard step above should have caught it — check for a race between this run and orchestrator-image.yml's deploy job."
              echo "Otherwise: check the synthetic heartbeat reached the event/task Service Bus queues (az rest GET .../queues/<name> --query properties.countDetails), and ca-orchestrator's own console logs for worker startup / message-receipt lines."
            fi
            if [ "$STATUS" = "Succeeded" ] || [ "$STATUS" = "Failed" ]; then
              break
            fi
            ATTEMPT=$((ATTEMPT + 1))
            if [ "$ATTEMPT" -le "$MAX_ATTEMPTS" ]; then
              sleep "$SLEEP_SECONDS"
            fi
          done
          if [ "$STATUS" != "Succeeded" ]; then
            echo "caj-orchestrator-smoke-test did not reach Succeeded (last status: $STATUS)"
            # L-0024: job logs show requires an explicit --container even for
            # a single-container job (matches smoke-test-job.bicep's
            # declared container name 'orchestrator-smoke-test').
            EXECUTION_NAME=$(az containerapp job execution list -g "$RESOURCE_GROUP" -n caj-orchestrator-smoke-test --query "[0].name" -o tsv 2>/dev/null || echo "")
            echo "--- caj-orchestrator-smoke-test log (execution: $EXECUTION_NAME) ---"
            az containerapp job logs show -g "$RESOURCE_GROUP" -n caj-orchestrator-smoke-test --execution "$EXECUTION_NAME" --container orchestrator-smoke-test || true
            echo "--- end of caj-orchestrator-smoke-test log ---"
            exit 1
          fi
      # -- session/s3-orchestrator: end --

  deploy-autopass:
    name: deploy (auto-pass — no infra changes detected)
    needs: what-if
    if: needs.what-if.outputs.can_autopass == 'true'
    runs-on: ubuntu-latest
    # Deliberately NO `environment:` here — that is the entire reason this
    # job exists separately from deploy-gated rather than as a conditional
    # environment expression on one job. AZURE_CLIENT_ID/AZURE_TENANT_ID/
    # AZURE_SUBSCRIPTION_ID are repo-level secrets, not environment-scoped —
    # the what-if job above already proves they're reachable with no
    # `environment:` at all — so nothing is lost by skipping it here; only
    # the required-reviewers protection rule is skipped, which is the point.
    steps: *deploy_steps
