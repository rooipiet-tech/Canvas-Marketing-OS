[project]
name = "canvas-marketing-os"
version = "0.1.0"
description = "Canvas Marketing OS — repo-root project metadata and shared tooling configuration."
requires-python = ">=3.12,<3.13"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]
