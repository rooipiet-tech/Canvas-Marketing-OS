[project]
name = "mcp-canva"
version = "0.1.0"
description = "mcp-canva — MCP server exposing template-locked design creation, bulk-create-from-CSV, and export against Canva's Connect API."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.111",
    "uvicorn>=0.30",
    "httpx>=0.27",
    "pyyaml>=6.0",
    "mcp-common",
]
