[project]
name = "mcp-web"
version = "0.1.0"
description = "mcp-web — MCP server exposing a rate-limited, egress-allow-listed URL fetch tool."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.111,<0.141",
    "uvicorn>=0.30,<0.52",
    "httpx>=0.27,<0.29",
    "pyyaml>=6.0,<7",
    "mcp-common",
]
